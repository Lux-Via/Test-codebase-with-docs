# Changelog

## [Unreleased]
- **@WeOnlyLiveOnce13 (DanTshisungu)**: feat: add inventory report HTML template and generate_report() method

## [0.2.0] — Source code upgrade
- Replaced function-based `calculator.py` with stateful `Calculator` class
- Replaced `greeter.py` with `inventory.py` — full `Inventory` and `Product` classes
- Added operation history, sqrt, power to Calculator
- Added search, low_stock, total_value to Inventory

## [0.1.0] — Initial release
- Added `calculator.py` with basic arithmetic
- Added `greeter.py` with greet and farewell